/** Placeholder admin login — real Supabase Auth form lands in M4. */
export default function AdminLoginPage() {
  return (
    <main style={{ padding: "var(--space-10)", textAlign: "center" }}>
      <h1 style={{ fontFamily: "var(--font-display)" }}>Admin login</h1>
      <p style={{ color: "var(--color-ink-soft)" }}>Coming in M4.</p>
    </main>
  );
}
