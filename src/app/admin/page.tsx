/** Placeholder admin overview — real dashboard lands in M4. */
export default function AdminOverviewPage() {
  return (
    <main style={{ padding: "var(--space-10)" }}>
      <h1 style={{ fontFamily: "var(--font-display)" }}>Overview</h1>
      <p style={{ color: "var(--color-ink-soft)" }}>Coming in M4.</p>
    </main>
  );
}
